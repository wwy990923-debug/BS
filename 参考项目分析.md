# 参考项目分析

基于[技术栈考量](./技术栈考量.md)中确定的全 Python 技术栈（PySide6 + faster-whisper + sounddevice + SQLite），梳理值得参考的类似开源项目。

---

## 一、最直接相关的组合参考（PySide6 + faster-whisper）

### 1. [CheshireCC/faster-whisper-GUI](https://github.com/CheshireCC/faster-whisper-GUI) ⭐~2,900

**最成熟、最值得重点阅读的项目。**

| 维度 | 与 WeldTrack 的对应关系 |
|------|--------------------------|
| GUI 框架 | PySide6，完全一致 |
| ASR 引擎 | faster-whisper，完全一致 |
| VAD | Silero VAD，完全一致 |
| 模型管理 | 模型下载/切换/量化，int8 量化可参考 |
| 多线程 | QThread 跑推理，不阻塞 GUI |

**可学习的内容**：PySide6 的 QThread 架构写法、faster-whisper 模型加载复用的最佳实践、VAD 参数调优、GUI 信号/槽模式如何接收推理结果。

**不足**：这是文件转写工具，不是实时录音→识别→填表的工作流，缺少"录入→确认→入库"的业务流水线。

---

### 2. [gurjar1/OmniDictate](https://github.com/gurjar1/OmniDictate)

**最接近 WeldTrack 的实时录音场景。**

| 维度 | 与 WeldTrack 的对应关系 |
|------|--------------------------|
| GUI 框架 | PySide6，完全一致 |
| ASR 引擎 | faster-whisper，完全一致 |
| 音频采集 | sounddevice，完全一致 |
| 录音模式 | PTT（按键说话）+ VAD（静音检测） |
| 平台 | Windows，一致 |
| 代码量 | 小，核心三个文件：`main_gui.py` / `core_logic.py` / `hotkey_listener.py` |

**可学习的内容**：实时麦克风采集 → faster-whisper 推理 → GUI 展示的完整链路，sounddevice 和 faster-whisper 如何在一个 PySide6 应用里协作。

**不足**：这是听写打字工具，没有结构化字段提取和业务入库流程。

---

### 3. [Moonfall2308/whisper-realtime-voice-to-text](https://github.com/Moonfall2308/whisper-realtime-voice-to-text)

**UI 交互参考价值高。**

| 维度 | 与 WeldTrack 的对应关系 |
|------|--------------------------|
| GUI 框架 | PySide6，完全一致 |
| 实时语音 | 支持实时转写 + 文件转写 |
| UI 特色 | 音频波形可视化、多语言切换 |
| 模型管理 | tiny / base / small / medium / large 可选 |

**可学习的内容**：录音状态反馈的 UI 设计——触摸屏需要大按钮、大字号、直观的状态提示，这里的波形/状态指示可以参考。

**不足**：使用 OpenAI Whisper 而非 faster-whisper，macOS 为主，Windows 适配需自行验证。

---

### 4. [LeoWang329/fatser-whisper-GUI](https://github.com/LeoWang329/fatser-whisper-GUI)

另一个 PySide6 + faster-whisper + WhisperX 的实现，可作为 CheshireCC 项目的补充参考。功能类似但实现细节不同，可以对比学习 QThread 的不同写法。

---

## 二、领域相关的参考（语音→结构化录入）

### 5. [speech-schema-filling](https://github.com/hampusnasstrom/speech-schema-filling)

**语音→结构化字段填充，模式上最接近 WeldTrack。**

- 场景：实验室人员口述实验记录 → Whisper 转写 → LLM 按 JSON Schema 填字段
- 管线：`Audio → Whisper → Structured JSON Fields → 入库`
- 与 WeldTrack 的管线 `语音 → 缺陷类型/返修方式/返修次数/处理结果 → SQLite` 完全同构

**可学习的内容**：如何把非结构化的语音文本映射到预定义字段字典、缺失字段如何提示人工补齐。

**不足**：依赖 Ollama + Llama3 做结构化填充，WeldTrack 用词典+正则就够了。它的 schema filling 概念可以参考，但实现用不着 LLM。

---

### 6. [FMEA_SupplyChain](https://github.com/gdg-charusat/FMEA_SupplyChain)

**场景接近：工业现场 + 语音录入缺陷描述。**

- 工厂工人对缺陷口述 → Whisper 转写 → FMEA 分析
- 与 WeldTrack 同为"工业现场、工人语音输入、质量相关"

**可学习的内容**：领域词汇的 prompt 偏置策略（与 WeldTrack `initial_prompt="焊接返修场景：气孔、夹渣..."` 思路一致）。

**不足**：基于 Streamlit（Web UI），不是 PySide6 桌面应用。

---

### 7. [pofice/voice-input-method](https://github.com/pofice/voice-input-method)

AI native 的跨平台离线语音输入法。可参考离线语音输入的用户体验设计。

---

### 8. [WhisperForge](https://github.com/WalksWithASwagger/whisperforge)

Whisper 转写 + 自定义 prompt + 结构化信息提取。如果后续想把词典匹配升级为更智能的提取方式，它的 custom prompt 策略值得参考。

---

## 三、推荐文章

- [Whisper + Custom Prompts — How We Turn Messy Voice Into Structured Data](https://dev.to/jakub_inithouse/whisper-custom-prompts-how-we-turn-messy-voice-into-structured-data-1a4a)：讲 Whisper 的 `prompt` 参数如何偏置领域词汇，把专有名词识别准确率从 65% 提到 88%。WeldTrack 的 `initial_prompt` 正是用了这个机制，这篇文章会帮助进一步调优。

---

## 四、建议的阅读顺序

优先级从高到低：

1. **OmniDictate** → 先看懂"实时录音→ASR→GUI"的最小闭环怎么跑
2. **faster-whisper-GUI** → 再学"模型管理 + QThread + 信号槽"的工业级写法
3. **speech-schema-filling** → 理解"语音→结构化字段"的流水线模式
4. **FMEA_SupplyChain** → 参考工业领域词汇的 prompt 偏置策略
5. **whisper-realtime-voice-to-text** → 借鉴实时录音的 UI 反馈设计

前两个项目代码量不大（OmniDictate 核心就 3 个文件），花半天到一天通读一遍，比从零搭架子节省很多踩坑时间。

---

## 五、快速对比总览

| 项目 | PySide6 | faster-whisper | 实时录音 | 文件转写 | VAD | 场景匹配度 |
|------|:-------:|:--------------:|:-------:|:-------:|:---:|:---------:|
| **faster-whisper-GUI** | ✅ | ✅ | ❌ | ✅ | ✅ | 高（架构参考） |
| **OmniDictate** | ✅ | ✅ | ✅ | ❌ | ✅ | 高（实时链路参考） |
| **whisper-realtime** | ✅ | ❌ | ✅ | ✅ | ❌ | 中（UI 参考） |
| **fatser-whisper-GUI** | ✅ | ✅ | ❌ | ✅ | ✅ | 中（补充参考） |
| **speech-schema-filling** | ❌ | ❌ | ❌ | ✅ | ❌ | 中（管线模式参考） |
| **FMEA_SupplyChain** | ❌ | ❌ | ✅ | ✅ | ❌ | 中（领域场景参考） |
| **voice-input-method** | ❌ | ❌ | ✅ | ❌ | ✅ | 低（UX 参考） |
| **WhisperForge** | ❌ | ❌ | ❌ | ✅ | ❌ | 低（prompt 策略参考） |

---

*文档版本：v1.0 | 日期：2026-05-28*
