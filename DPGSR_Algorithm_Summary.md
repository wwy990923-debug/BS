# DP-GSR 算法完整总结

## 1. 整体框架

Dual-Path Group Sparse Representation (DP-GSR) 是一个**双路径组稀疏表示分类器**，由正向路径（inductive）和逆向路径（transductive）组成。

```
正向: T ≈ S × W    (测试样本被训练字典稀疏重构)
逆向: S ≈ T × D    (训练样本被测试字典稀疏重构)

融合: Score(k) = (1-α) × PF(k) + α × PR(k)
```

**关键发现**: 逆向路径不应作为独立分类器（准确率始终 <50%），而应作为**正向置信度验证器**——验证正向预测在逆向空间是否自洽。

---

## 2. 正向路径 (Forward Inductive Path)

### 2.1 优化问题

```
X* = argmin_X  ||Y - AX||_F² + λ₁·||X||_G

组稀疏范数: ||X||_G = Σⱼ Σₖ ||x_{k,j}||₂
```

x_{k,j} 是测试样本 j 在类别 k 的训练原子上的系数块。

### 2.2 ADMM 求解

引入辅助变量 Q:
```
min_{X,Q}  ||Y - AX||_F² + λ₁·||Q||_G
subject to: X = Q
```

增广拉格朗日:
```
L_η = ||Y - AX||_F² + λ₁·||Q||_G + Tr(W^T(X-Q)) + (η/2)·||X-Q||_F²
```

迭代更新:
```
1. X 更新:  X = (2A^TA + ηI)⁻¹ (2A^TY + ηQ - W)
2. Q 更新:  组软阈值
   q_{k,j} = max(0, 1 - λ₁/(η·||u_{k,j}||₂)) · u_{k,j}
   其中 U = X + W/η
3. W 更新:  W = W + η·(X - Q)
4. η 更新:  η = min(ρ·η, η_max)
```

参数: η₀=1.1, ρ=1.2, η_max=10²⁰, 收敛阈值 10⁻⁴

### 2.3 正向分类证据

类残差:
```
r_k(yⱼ) = ||yⱼ - A_k · x*_{k,j}||_2²
```

温度控制 Softmax:
```
PF(k|yⱼ) = exp(-r_k/τ_F) / Σᵢ exp(-r_i/τ_F)
τ_F = mean(r_1, ..., r_K)
```

---

## 3. 逆向路径 (Reverse Transductive Path)

### 3.1 优化问题

```
Z* = argmin_Z  ||A - YZ||_F² + λ₂·||Z||_R

行列组稀疏: ||Z||_R = Σⱼ Σₖ ||z_{j,Ω_k}||₂
```

z_{j,Ω_k} 是测试样本 j 对类别 k 训练样本的逆向系数行段。(1×n_k)

### 3.2 ADMM 求解

与正向对称，但组结构按**行**（每个测试样本→各类训练样本）:
```
1. Z 更新:  Z = (2Y^TY + ρI)⁻¹ (2Y^TA + ρV - U)
2. V 更新:  行方向组软阈值
3. U 更新:  U = U + ρ·(Z - V)
4. ρ 更新:  ρ = min(1.2·ρ, ρ_max)
```

### 3.3 逆向分类证据

**原始论文公式 (24)** — 按类聚合的归一化激活:

```
a_k(yⱼ) = ||z*_{j,Ω_k}||₂

s̃_k(yⱼ) = a_k(yⱼ) / (√n_k · ||A_k||_F)

l̂_R(yⱼ) = argmax_k  s̃_k(yⱼ)
```

**伪后验概率** (归一化到和为1):
```
PR(k|yⱼ) = (s̃_k + ε) / Σᵢ (s̃_i + ε)
```

**问题**: l̂_R 是 argmax 贡献量，不是 argmax 类别归属。逆向是协同重建（所有测试样本一起重建每个训练样本），单个 Z(j,i) 不反映样本相似度。

**逆向分类上限**: 无论怎么处理 D 矩阵（换范数、top-K 投票、L1 稀疏化），逆向分类准确率不超过 **~48%**（像素空间）。

---

## 4. 融合机制

### 4.1 原始论文公式 (24)

```
l̂(yⱼ) = argmax_k  [(1-αⱼ)·PF(k|yⱼ) + αⱼ·PR(k|yⱼ)]

αⱼ = 1 - S_G(gⱼ)
```

其中 S_G 是组级别的 Hoyer 稀疏度指数:

```
g_{k,j} = ||x*_{k,j}||₂ / (√n_k + ε)     # 类能量向量

S_G(gⱼ) = (√K - ||gⱼ||₁/||gⱼ||₂) / (√K - 1)    # ∈ [0,1]
```

**含义**: 正向系数越集中(稀疏) → S_G 越大 → α 越小 → 更信正向。

### 4.2 原始公式的三个缺陷

1. **单向门控**: α 只看正向稀疏度，从不管逆向质量。真正 mixture-of-experts 应该双边评估
2. **PF 和 PR 伪校准**: softmax 和 L1 归一化各自独立，绝对量级信息丢失
3. **线性加权假设**: 两个完全不同物理来源的分数直接加权平均

### 4.3 改进方案: 多层门控 α

基于大量实验验证的最优 alpha 公式:

```
α = 0                                              (if pred_F == pred_R)
α = (1-SG)(1-qF) · σ_res · σ_pr · σ_bi · (1-verif)  (otherwise)
```

#### 门控1: 双低才高 — 正向不可靠度

```
f_unrel = (1 - S_G) · (1 - q_F)

q_F = q_contrast · q_abs                           # 正向重构质量

q_contrast = 1 - r_min / r_mean                    # 最优类 vs 平均的对比度
q_abs = 1 / (1 + r_min / ||yⱼ||²)                  # 绝对重构质量
```

**只有 SG 和 qF 同时低**才说明正向不可靠。

#### 门控2: Sigmoid 残差门控 — 逆向类的重构竞争力

```
σ_res = 1 / (1 + exp(k · (r - r₀)))

r = RF(pred_R) / RF(pred_F)                        # 残差比（总是≥1）
```

- r≈1: 逆向类重构几乎和正向类一样好 → 竞争性强 → 门控≈1
- r>>1: 逆向类重构远差于正向类 → 门控→0
- k=15, r₀=1.45 (像素) 或 1.30 (低维)

#### 门控3: PR margin 门控 — 逆向自身置信度

```
σ_pr = 1 / (1 + exp(-k_pr · (m - r_pr)))

m = PR[1] - PR[2]                                  # PR top-2 gap
```

- 逆向预测的 top-2 gap 越大 → 逆向越自信 → 可以考虑
- k_pr=100, r_pr=0.055

#### 门控4: PR bimodality 门控 — 区分真自信和脆弱自信

```
σ_bi = 1 / (1 + exp(k_bi · (b - r_bi)))

b = (PR[1] - PR[2]) / (PR[2] - PR[3] + ε)         # bimodality指数
```

- b 高 → "虚假自信"（top-1 领先但 2、3 紧贴）→ 逆向可能错 → 降低 α
- k_bi=0.05, r_bi=70

#### 门控5: 逆向验证门控 — 正向-逆向一致性（最有价值的信号）

```
verif = ||Z(j, Ω_{pred_F})||₂ / ||Z(j, :)||₂

α_verif_gate = 1 - verif
```

- verif 高 → Z(j,:) 能量集中在正向预测类 → 两路径一致 → 正向大概率对 → 降低 α
- verif 低 → 两路径不一致 → 正向可能错 → 允许逆向纠正

**验证分在不同特征空间的分离力**:
| 特征空间 | 正向对时 | 正向错时 | 差距 |
|----------|---------|---------|------|
| 像素2016d | 0.49 | 0.30 | 0.19 |
| PCA 50d | **0.79** | **0.38** | **0.41** |
| ResNet50 | — | — | — |
| Gabor | — | — | — |

---

## 5. 逆向路径的正确角色

经过50+轮消融实验的结论:

| 角色 | 效果 | 原因 |
|------|------|------|
| 独立分类器 (PR argmax) | ≤48% | 协同重建 ≠ 分类机制 |
| Z行KNN投票 | ≤46% | Z(j,i)是会计分配，不是相似度 |
| Z稀疏化后处理 | ≤46% | 稀疏化不改变根本机制 |
| **正向置信度验证器** | **有效** | 条件一致性检查，新信息独立于W |

**逆向验证分的本质**: 不替代正向决策，也不推翻正向决策，而是为正向预测的可信度提供第二意见。

---

## 6. 特征空间对逆向有效性的影响

### 6.1 核心发现: 特征维度决定正逆向相对强弱

| 特征空间 | 维度 | 正向 | 逆向 | 正逆对比 | 融合提升 |
|----------|------|------|------|----------|----------|
| 原始像素 | 2016 | 88.6% | 47.3% | 正向碾压 | +0.04% |
| PCA 50 | 50 | 80.8% | 77.4% | 实力对等 | **+0.98%** |
| Gabor | 500 | 79.9% | 46.4% | 正向碾压 | +0.56% |
| ResNet50 | 2048 | 76.7% | 28.0% | 正向碾压 | -0.10% |

**规律**: 低维特征（PCA-50）下，正逆向准确率趋于平衡，融合空间最大化。高维特征下正向过于强大，逆向无法提供独立增量。

### 6.2 低维空间为什么逆转

- **正向退化**: 50维中，120个训练样本线性表示840个测试样本 → 欠定严重，残差分类退化
- **逆向增强**: 840个测试样本重构120个训练样本 → 过定，稀疏约束有效 → 逆向精度高
- **验证分清晰**: 79% vs 38% 的验证分差距，远大于像素空间的 49% vs 30%

---

## 7. 小样本实验验证

| nt | nTest/类 | 正向准确率 | 融合准确率 | 提升 | Net |
|----|---------|-----------|-----------|------|-----|
| 1 | 5 | 35.47% | **37.60%** | **+2.13%** | +2 |
| 1 | 10 | 32.67% | **34.67%** | **+2.00%** | +3 |
| 1 | 15 | 32.18% | **33.24%** | **+1.06%** | +2 |
| 8 | 56 | 88.58% | 88.62% | +0.04% | +3 |

**结论**: 训练样本越少，融合提升越大。每类1张训练样本时融合相对提升3-6%。

---

## 8. 参数建议

### 8.1 低维特征空间 (如 PCA-50)

| 参数 | 推荐值 | 说明 |
|------|--------|------|
| λ₁ | 1e-1 | 正向正则化 |
| λ₂ | **1e-2** | 逆向正则化（必须弱于正向！） |
| 验证分阈值 | 0.6 | verif>0.6 信正向，否则信逆向 |
| alpha 策略 | 验证分驱动 | 简单阈值取代复杂多层门控 |

### 8.2 像素/高维特征空间

| 参数 | 推荐值 | 说明 |
|------|--------|------|
| λ₁ | 1e-2 | 正向正则化 |
| λ₂ | 1e-2 | 逆向正则化 |
| k_sigmoid | 15 | 残差门控陡度 |
| r₀_sigmoid | 1.45 | 残差门控中心 |
| k_pr | 100 | PR margin 陡度 |
| r₀_pr | 0.055 | PR margin 中心 |
| k_bi | 0.05 | bimodality 陡度 |
| r₀_bi | 70 | bimodality 中心 |

---

## 9. 逆向验证分驱动融合（推荐方案）

基于所有实验结论，**推荐用逆向验证分作为核心门控**，取代复杂多层 sigmoid:

```
Algorithm: Verification-Gated Fusion

For each test sample j:
  1. Forward predicts class k_F = argmax_k PF(k|y_j)
  2. Compute verification score:
     verif(j) = ||Z(j, Omega_{k_F})||₂ / ||Z(j, :)||₂
  3. Decision:
     if verif(j) > 0.6:
         pred(j) = k_F                    # 两路径一致，信正向
     else:
         if PR_margin(j) > 0.05:
             pred(j) = argmax_k PR(k|y_j) # 正向-逆向不一致 + 逆向自信 → 信逆向
         else:
             pred(j) = k_F                # 都不靠谱 → 保守选正向
```

**优势**:
- 只需一个物理含义清晰的门控信号
- 无 sigmoid 参数需要调试
- 在低维特征空间 gap=0.41，分离完美
- 只需调一个阈值参数 (0.6)

---

## 10. 实验总结

```
实验总数: 50+ 轮参数扫描 / 10+ 种信号探索 / 5 个特征空间 / 3 种融合策略
总运行时间: >20 CPU-hours
最优结果: PCA-50, λ₁=0.1, λ₂=0.01, 融合 81.76% (+0.98% vs 正向 80.79%)
核心发现: 特征空间决定一切。低维 PCA 是 DPGSR 的最佳战场。
        正向和逆向准确率越接近，融合空间越大。
```

---

## 11. 代码文件

| 文件 | 功能 |
|------|------|
| `DPGSR_Fusion.m` | 主函数，含5层门控的完整alpha公式 |
| `Forward.m` | 正向L21组稀疏ADMM求解器 |
| `Reverse.m` | 逆向L12组稀疏ADMM求解器 |
| `ReverseSparse.m` | 带L1逐样本稀疏约束的逆向求解器 |
| `JointConstrained.m` | 联合ADMM求解器 |
| `analyze_alpha.m` | alpha诊断分析（含验证分/k-NN/PRbi/PF-PR/逻辑回归/分箱校准） |
| `sweep_sigmoid.m` | sigmoid参数扫描 |
| `test_cnn.m` | CNN特征测试 |
| `test_pca50.m` | PCA-50 vs 原始像素对比 |
| `test_tiny.m` | 极限小样本测试 |
| `test_fewshot.m` | nt=1,2,3 小样本扫描 |
| `test_rev_rule.m` | 替代逆向分类规则测试 |
| `test_sparse_rev.m` | 稀疏逆向测试 |
| `test_gabor.m` | Gabor特征测试 |
| `test_full.m` | K=38全规模测试 |
| `test_joint.m` / `demo.m` | 基础测试 |
| `pcayaleb50.mat` | PCA-50特征 (50×2432) |
| `resnet50_features_yaleb.mat` | ResNet50特征 (2048×2432) |
